      - name: Deploy Sovereign Treasury Engine
        env:
          POLYGON_RPC_URL: ${{ secrets.POLYGON_RPC_URL }}
        run: python deploy_treasury.py

      - name: Vérifier le Solde de Trésorerie
        env:
          POLYGON_RPC_URL: ${{ secrets.POLYGON_RPC_URL }}
        run: python check_balance.py
